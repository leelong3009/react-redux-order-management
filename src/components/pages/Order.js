import React, { Component } from 'react';
import { connect } from 'react-redux';
import { fetch } from '../../actions/customerAction';
import { Table } from '../table/Table';

const Order = () => <h1>Order</h1>;

export default Order;
